type Article {
  id: ID!
  title: String!
  content: String!
  authorId: String!
  categoryId: String!
  hastags: [String]
  createdAt: String!
  updatedAt: String!
  view: String!
  description: String!
  imageDescription: String
}

type Query {
  get_most_view_articles(amount: Int!): [Article!]!
}

type Mutation {
  create_article(
    title: String
    content: String
    categoryId: String
    hastags: [String]
    description: String
    imageDescription: String
  ): Article!
}
